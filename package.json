{
  "private": true,
  "name": "milesj-root",
  "scripts": {
    "eslint": "beemo eslint .",
    "prettier": "beemo prettier \"./{configs,dotfiles,scripts}/*.{js,json,md,yml,yaml}\"",
    "test": "yarn run eslint",
    "prereleassse": "yarn test",
    "release": "lerna publish"
  },
  "dependencies": {
    "lerna": "^3.2.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "beemo": {
    "module": "@local",
    "drivers": [
      "eslint",
      "prettier"
    ],
    "eslint": {
      "rules": {
        "no-var": "off",
        "no-console": "off",
        "promise/always-return": "off"
      }
    }
  },
  "browserslist": [
    "last 2 versions"
  ]
}
